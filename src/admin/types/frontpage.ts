export interface FrontpageSettings {
  hero: {
    text: string;
  };
  featuredCollections: {
    heading: string;
    body: string;
    ctaText: string;
  };
  newArrivals: {
    title: string;
  };
}
